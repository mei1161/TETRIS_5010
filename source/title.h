/*#pragma once
#include <d3d11.h>
#include "release.h"

////////////////////////////////////
// class
////////////////////////////////////
class Title
{
private:
	ID3D11ShaderResourceView* texture_;     // テクスチャ

public:
	Title() :texture_(NULL) {}            // コンストラクタ
	bool init();                            // 初期化
	bool update();                          // 更新
	void draw();                            // 描画
	void destroy();                         // 破棄
};*/